sudo: false
language: elixir
git:
  depth: 3
elixir:
  - 1.4
  - 1.5
otp_release:
  - 19.3
  - 20.0
env:
  - MIX_ENV=test VERBOSE_TESTS=true
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix eqc.install --mini --force
script:
  - mix test
